---
marp: true
paginate: true
header: "System Analyst Bootcamp"
footer: "BPMN: Как показать процесс (часть 2)"
style: |
  @import url('https://fonts.googleapis.com/css2?family=Ubuntu:wght@400;500;700&family=JetBrains+Mono&family=Source+Serif+4:wght@400;600&family=Dancing+Script:wght@400;700&display=swap');

  section {
    font-family: 'Ubuntu', sans-serif;
    background: #F9F5F0;
    color: #424242;
    padding: 40px;
  }

  h1 {
    color: #2C3E50;
    border-bottom: 3px solid #3498DB;
    padding-bottom: 10px;
  }

  h2 { color: #2C3E50; }
  strong { color: #E74C3C; }

  code {
    font-family: 'JetBrains Mono', monospace;
    background: #EDEAE5;
    padding: 2px 8px;
    border-radius: 4px;
    color: #424242;
  }

  pre {
    background: #2C3E50;
    color: #F9F5F0;
    padding: 20px;
    border-radius: 8px;
  }

  pre code {
    background: transparent;
    color: inherit;
  }

  blockquote {
    border-left: 4px solid #3498DB;
    padding-left: 20px;
    color: #6B6B6B;
    font-style: italic;
  }

  table {
    width: 100%;
    border-collapse: collapse;
  }

  th {
    background: #2C3E50;
    color: #F9F5F0;
    padding: 12px;
  }

  td {
    border: 1px solid #DCDCDC;
    padding: 10px;
  }

  section.lead {
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-align: center;
  }

  section.lead h1 {
    border-bottom: none;
    padding-bottom: 0;
  }

  section.lead strong {
    font-size: 1.4em;
    color: #2C3E50;
  }

  section.title {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 60px;
  }

  section.title header,
  section.title footer {
    display: none;
  }

  section.title .course {
    font-family: 'Source Serif 4', serif;
    font-size: 45px;
    font-weight: 600;
    color: #2C3E50;
    margin-bottom: 8px;
  }

  section.title .author {
    font-family: 'Source Serif 4', serif;
    font-size: 25px;
    font-weight: 400;
    color: #6B6B6B;
    margin-bottom: 40px;
  }

  section.title h1 {
    font-family: 'Dancing Script', cursive;
    font-size: 75px;
    font-weight: 400;
    color: #2C3E50;
    border-bottom: none;
    padding-bottom: 0;
    margin: 0;
  }
---

<!-- _class: title -->
<!-- _paginate: skip -->

<div class="course">SYSTEM ANALYST</div>
<div class="author">presented by Ayub</div>

# BPMN: События и практика

---

<!-- _class: lead -->

# Этап 4
## События в потоке

---

<!-- _class: lead -->

**Заявитель подал заявление на замену ВУ.**

По регламенту МВД отвечает на запрос о лишении **до 5 рабочих дней**.

Куда на диаграмме эти 5 дней ожидания?

Task? Gateway? Стрелка?

<!--
Teacher Notes:
- Дай 20 секунд. Пусть мучаются.
- Ожидаемые ответы: "Task ▢ Ждать 5 дней", "Gateway", "просто стрелка"
- Не давай ответ — подведи: "ни один из пройденных элементов не подходит честно"
- ~1 мин
-->

---

# Чего не хватает в нашем арсенале

Три ситуации, которые Task и Gateway **не умеют** показать:

- Процесс **ждёт** время — 5 дней на ответ МВД
- Извне **прилетает** сообщение — Минздрав подтвердил справку
- Внутри задачи **сломалось** — eGov зависла во время проверки

> **Task** = *мы* делаем.
> **Gateway** = *мы* выбираем по данным.
> А здесь — *с нами случается*.

Для этого BPMN даёт третий класс элементов — **Event (событие)**.

<!--
Teacher Notes:
- Ключевая мысль: Task и Gateway описывают наши активные действия. Event — реакция на внешний мир или время.
- ~2 мин
-->

---

# Task vs Gateway vs Event

|               | Кто активен              | Пример в ЦОН                                  |
| ------------- | ------------------------ | --------------------------------------------- |
| **Task ▢**    | Мы работаем              | Регистратор проверяет паспорт                 |
| **Gateway ◇** | Мы решаем по данным      | Возраст ≥ 18?                                 |
| **Event ○**   | С нами что-то происходит | Пришёл ответ МВД / истекли 7 дней / АИС упала |

**Запомни одну строчку:**

> **Gateway смотрит в данные. Event реагирует на мир.**

<!--
Teacher Notes:
- Эта таблица — якорь на весь этап 4. Если студент потеряется — возвращай сюда.
- Подчеркни: Gateway нужны данные в руках. Event — срабатывает сам от времени/сообщения/сбоя.
- ~2 мин
-->

---

<!-- _class: lead -->

# Быстрый квиз

**Чем это на диаграмме — Task, Gateway или Event?**

1. «Инспектор сверяет фото с базой» — что это?
2. «Пришёл ответ из реестра МВД» — что это?
3. «Прошло 7 дней после провала теории» — что это?
4. «Справка действительна?» — что это?
5. «eGov зависла прямо во время проверки» — что это?

<!--
Teacher Notes:
- Дай 1 минуту. Пусть отвечают хором или по рукам.
- Ответы: 1) Task, 2) Event, 3) Event, 4) Gateway, 5) Event
- Разбери любые споры — особенно п.2 vs п.4: "пришёл ответ" — факт мира (Event); "справка действительна" — проверка данных (Gateway).
- ~2 мин
-->

---

# Timer Event

**Timer Event** — поток ждёт назначенное время

- Круг с иконкой часов: `○⏱`
- Не "задача", а событие — никто ничего не делает
- Может задавать: интервал, конкретную дату, цикл

**Пример:**
```
○ Start
    ↓
▢ Сдать теорию
    ↓
◇× «Сдал?»
  нет ↓
 ○⏱ 7 дней
    ↓
▢ Пересдать теорию
```


<!--
Teacher Notes:
- Timer Event делает явным то, что иначе "живёт в голове" сотрудников
- Виды Timer: 7 дней (7d), 30 дней (30d), 6 месяцев (6m), конкретная дата
- Элементы для bpmn.io: Start Event → Task "Сдать теорию" → Exclusive Gateway "Сдал?" → Intermediate Timer Catch Event "7 дней" → Task "Пересдать теорию"
- ~3 мин
-->

---

<!-- _class: lead -->

**Мы отправили запрос во внешнюю систему.**

Через какое-то время оттуда прилетает ответ.

Это Receive Task? Или что-то ещё?

<!--
Teacher Notes:
- Дай 20 секунд
- Ожидаемые ответы: "Receive Task", "событие", "не знаю"
- Не давай ответ сам — просто скажи: "оба варианта существуют, сейчас разберёмся в чём разница"
- ~1 мин
-->

---

# Message Event (intermediate)

**Message Event** — момент, когда из-за границы процесса прилетает сообщение

- Круг с иконкой конверта: `○✉`
- Не задача — никто ничего не делает, процесс просто ждёт
- Часто стоит после Send Task

**Пример:**
```
▢ Принять медсправку
        ↓
📤 Отправить запрос в Минздрав
        ↓
   ○✉ Ответ получен
        ↓
  ◇× «Справка действительна?»
```

> Message Event = «что-то случилось с процессом». Receive Task = «мы активно ждём».

<!--
Teacher Notes:
- Не торопись объяснять разницу сам — она раскроется на следующем слайде через вопрос
- Элементы для bpmn.io: User Task "Принять медсправку" → Send Task "Отправить запрос в Минздрав" → Intermediate Message Catch Event "Ответ получен" → Exclusive Gateway "Справка действительна?"
- ~2 мин
-->

---

<!-- _class: lead -->

**Оба варианта работают. Но когда один лучше другого?**

Подумай: что изменится, если нам нужно —

«Если ответ не пришёл за 2 часа — идём дальше без него»?

<!--
Teacher Notes:
- Дай 20 секунд
- Ожидаемые ответы: "таймер", "ещё один шлюз", "не знаю"
- Подведи: чтобы поставить таймер на ожидание — нужен Receive Task, не Event. Только к Task можно прицепить Boundary Event.
- ~1 мин
-->

---

# Receive Task vs Message Catch Event

Оба ждут сообщения. Но структурно — разные элементы.

|                          | `○✉` Message Catch Event | `▢✉` Receive Task |
| ------------------------ | ------------------------ | ----------------- |
| Смысл                    | *что-то произошло*       | *мы активно ждём* |
| Boundary Timer / Error   | ❌                        | ✅                 |
| Loop (несколько ответов) | ❌                        | ✅                 |
| Event-Based Gateway      | ✅ обязательно            | ❌                 |

**Правило:**
- По умолчанию → `○✉` Message Catch Event
- Нужен таймаут или ошибка на ожидание → `▢✉` Receive Task

<!--
Teacher Notes:
- Event-Based Gateway студенты ещё не проходили — не углубляйся, просто упомяни как факт
- Главный вывод: таймаут на ожидание = Receive Task + Boundary Timer
- ~3 мин
-->

---

<!-- _class: lead -->

**Ответ пришёл. Справка действительна.**

Процесс продолжается.

Но что если не ответ, а сама система зависла прямо во время работы?

<!--
Teacher Notes:
- Это переход к Boundary Error Event — проблема уже не в ожидании ответа, а в сбое внутри задачи
- ~30 сек
-->

---

<!-- _class: lead -->

**Заявитель пришёл в ЦОН. На входе — терминал электронной очереди.**

Обычно терминал печатает талон с номером: `A-047`. Но сегодня queue-service упал.

Очередь из 30 человек. Домой никого не отправишь.

В реальности — план Б: оператор в зале выдаёт бумажные талоны вручную и ведёт список номеров в журнале.

Как показать «если задача сломалась — уходим вбок»?

<!--
Teacher Notes:
- Дай 20 секунд
- Подведи: это не XOR (выбор не зависит от данных), это аварийный выход из задачи
- ~1 мин
-->

---

# Boundary Error Event

**Boundary Error Event** — аварийный выход из задачи

- Круг с молнией ⚡, прицеплен к границе задачи
- Если внутри задачи случилась ошибка — поток уходит по альтернативной ветке
- Сама задача прерывается

**Пример: выдача талона электронной очереди**
```
○ Заявитель вошёл в зал
         ↓
👤 Выбрать услугу на терминале
         ↓
   ┌────────────────────┐
   │ ⚙ queue-service    │⚡ ─→ ✋ Оператор выдаёт
   │   печатает талон   │       бумажный талон
   │   "A-047"          │       и пишет номер
   └────────────────────┘       в журнал
         ↓ (норма)                   ↓
   ▢ Ожидание вызова       ▢ Ожидание вызова
         на табло               по списку оператора
```

*queue-service упал — очередь не останавливается, операторы переходят на ручной режим.*

> Boundary Event — это "запасной выход" из горящего здания.

<!--
Teacher Notes:
- Это первый "не последовательный" элемент — событие сидит на границе, а не в потоке
- Реальный сценарий: queue-service — микросервис, падает вместе с сетью или БД; операторы обучены переходить на ручной режим
- Виды граничных событий: Error, Timer (таймаут на саму задачу), Message, Cancel
- Элементы для bpmn.io: Start Event "Заявитель вошёл в зал" → User Task "Выбрать услугу на терминале" → Service Task "queue-service печатает талон" (с Boundary Error Event на границе) → норма: Task "Ожидание вызова на табло" / по ошибке: Manual Task "Оператор выдаёт бумажный талон и пишет номер в журнал" → Task "Ожидание вызова по списку оператора"
- ~3 мин
-->

---

<!-- _class: lead -->

**Проверка МВД вернула: заявитель в розыске.**

Это не просто «отказ по текущему окну». Эту новость должны узнать сразу все:

- **Охрана ЦОН** — задержать до приезда наряда
- **Соседние окошки** — у него могут быть ещё заявления (паспорт, прописка) — заблокировать
- **Начальник смены** — вызвать наряд, составить акт
- **Архив** — пометить дело красным флагом

Мы не знаем заранее, кто именно отреагирует — просто объявляем факт.

Это «адресное письмо» одному получателю — или «объявление по громкой связи»?

<!--
Teacher Notes:
- Дай 20 секунд
- Подведи: разные типы событий для разных моделей оповещения
- ~1 мин
-->

---

# Signal Event

**Signal** — широковещательный сигнал, без конкретного адресата
- Круг с треугольником: `○▲`
- Ловят все, кто подписан на этот сигнал

---

# Signal в e-commerce: `OrderPaid`

В интернет-магазине клиент оплатил заказ. Платёжный шлюз вернул `success`.

```
⚙ Списать деньги с карты
        ↓
⚙ Ответ шлюза: success
        ↓
   ○▲ Signal "OrderPaid"
```

**Кто ловит этот сигнал — один из десятков подписчиков:**

- 📦 **Warehouse** — резервирует товар, печатает стикер
- 🚚 **Shipping** — создаёт накладную, вызывает курьера
- ⭐ **Loyalty** — начисляет бонусные баллы
- 📧 **Email-сервис** — отправляет чек и трек-номер
- 📊 **Analytics** — пишет метрику выручки
- 🔍 **Fraud** — сканирует на подозрительные паттерны

> Платёжный сервис **не знает** про склад, доставку и лояльность. Он просто кричит `OrderPaid` — а подписчики сами решают, что с этим делать.

<!--
Teacher Notes:
- Это каноничный пример event-driven архитектуры в e-commerce (Shopify, Amazon, Stripe webhooks)
- Ключевой момент: завтра маркетинг подключит ещё один сервис ("рекомендации"), он просто подпишется на OrderPaid — платёжный сервис трогать не нужно. Это суть Signal.
- Сравни с Message: если бы платёжный сервис слал 6 отдельных сообщений — пришлось бы хардкодить всех адресатов
- Элементы для bpmn.io: Service Task "Списать деньги с карты" → Service Task "Ответ шлюза: success" → Intermediate Signal Throw Event "OrderPaid"
- ~3 мин
-->

---

# Signal в ЦОН: `Розыск`

```
⚙ Запрос в реестр МВД
        ↓
⚙ Ответ: статус заявителя
        ↓
  ◇× «В розыске?»
   да ↓
   ○▲ Signal "Розыск"
   (ловят: охрана, соседние окошки,
    начальник смены, архив)
```

> ⚠️ **Важно:** просто вернуть заявителю «отказ» и отпустить — это **глупо и опасно**. Человек в розыске не должен уйти из ЦОН свободно. Signal запускает реальные действия: охрана задерживает, соседние окошки блокируют его другие заявления, начальник вызывает наряд.

> Signal — "крикнуть всем". Message — "адресное письмо одному получателю".

<!--
Teacher Notes:
- Главная мысль: Signal — это не "красивый способ показать отказ". Это механизм запуска реакций у тех, кто подписан.
- Подчеркни: если аналитик рисует "в розыске → End Event" и на этом всё — он не понял задачу. Процесс должен иметь последствия за пределами текущей ветки.
- Ключевое отличие Signal vs Message:
  - Message — точка-точка, один конкретный получатель (мы знаем кому)
  - Signal — широковещание, ловят все кто подписан (мы не знаем кто именно)
- Почему не Message: адресатов несколько, состав может меняться (завтра подключится ещё одна система), и отправитель не должен про них знать
- Элементы для bpmn.io: Service Task "Запрос в реестр МВД" → Service Task "Ответ: статус заявителя" → Exclusive Gateway "В розыске?" → Intermediate Signal Throw Event "Розыск"
- ~3 мин
-->

---

<!-- _class: lead -->

**Отказ из-за лишения. Дело закрывается.**

Но мы только что запустили AND-split с тремя параллельными ветками: проверка прав, проверка справки, формирование группы.

Если поставить обычный End Event — он закроет только свою ветку. А остальные продолжат тикать.

Как сказать «всё, занавес — гасим даже параллельные ветки»?

<!--
Teacher Notes:
- Дай 20 секунд. Это редкий, но важный случай.
- ~1 мин
-->

---

# Terminate End Event

**Terminate End** — мгновенно убивает ВЕСЬ процесс

- Жирный круг с закрашенным кружком внутри: `◉●`
- Гасит все активные параллельные ветки
- Используется при «фатальных» отказах

**Пример:**
```
         ◇+ AND-split
        /      |       \
   ⚙ Реестр  ✋ Пошлина  👤 Группа
   МВД        принята    сформирована
      ↓
  ◇× «Лишён?»
   да /     \ нет
      ↓      ↓
   ◉● Terminate    (в AND-join)
```

> Обычный End — закрывает свою ветку. Terminate — закрывает весь процесс, включая параллельные.

<!--
Teacher Notes:
- Predict: студенты не понимают разницу. Объясни на примере: если у нас 3 параллельные ветки и в одной обычный End — две другие продолжат работать. Terminate — убивает всё.
- Элементы для bpmn.io: Parallel Gateway (AND-split) → 3 ветки: Service Task "Реестр МВД", Manual Task "Принять пошлину", User Task "Сформировать группу" → ветка МВД: Exclusive Gateway "Лишён?" → да: Terminate End Event / нет: Parallel Gateway (AND-join) вместе с другими ветками
- ~3 мин
-->

---

# Module Check: события

1. **Чем** Timer Event лучше, чем Task `▢ Подождать 7 дней`?
2. **Где** на диаграмме рисуется Boundary Error Event?
3. **В чём разница** между Message и Signal?
4. **Когда** нужен Receive Task вместо Message Catch Event?
5. **Когда** обычный End Event не подходит и нужен Terminate?

<!--
Teacher Notes:
- Дай 5 минут
- Ответы:
  1. Timer — стандартный значок, понятный всем; Task с "подождать" — это костыль и не показывает что время это причина продолжить
  2. На границе задачи (отсюда название "boundary")
  3. Message — адресное письмо: один отправитель, один получатель. Signal — крик в эфир: ловят все подписанные.
  4. Когда нужен таймаут или ошибка на само ожидание — Boundary Event прицепляется только к Task
  5. Когда нужно убить параллельные ветки одновременно
- ~5 мин
-->

---

<!-- _class: lead -->

# Микропрактика 4: события

**Карандаш, 6 минут. Выбери ОДИН сюжет:**

- 🔑 **Сброс пароля по email** — Message Start «запрос сброса» → Timer «ссылка живёт 15 минут» → Boundary Error «токен невалиден»
- ✈️ **Бронирование авиабилета** — Timer «10 минут на оплату», Boundary Error «карта отклонена», Signal «рейс отменён» → Terminate всех параллельных веток
- 🚨 **SLA на инцидент в техподдержке** — Boundary Timer «не отвечено 4 часа → эскалация тимлиду»

**Обязательно:** минимум 2 разных события (Timer, Message, Boundary Error, Signal или Terminate).

<!--
Teacher Notes:
- Самое сложное упражнение первой части. Многие застрянут — это нормально.
- Подсказка: Boundary Error рисуется НА границе задачи, а не в потоке.
- Типовые ошибки: моделируют ожидание как Task ▢ "подождать 15 минут", путают обычный End с Terminate.
- ~6 мин
-->

---

<!-- _class: lead -->

# Поменяйтесь листами с соседом

**3 минуты на разбор:**

- Timer нарисован как событие (○⏱), а не как Task «подождать»?
- Boundary Event сидит НА границе задачи?
- Если есть параллельные ветки и критическая ошибка — используется Terminate?

**Верни лист с одной правкой и одной похвалой.**

<!--
Teacher Notes:
- После peer review — разбери 2 самые частые ошибки у доски.
- Этот ритуал закрывает теоретическую часть урока. Дальше — большая практика по Мобильный ЦОН.
- ~6 мин
-->

---

<!-- _class: lead -->

# Мини-практика
## Соберём всё, что выучили

**Возьми лист бумаги. Нарисуй сам.**

<!--
Teacher Notes:
- Это закрепление перед второй частью лекции
- Дай 10-15 минут на индивидуальную работу, потом 10 минут на разбор у доски
- ~25 мин
-->

---

# Задание: упрощённый процесс выдачи ВУ

**Нарисуй на листе диаграмму со следующими правилами:**

1. Старт — заявитель пришёл в Мобильный ЦОН
2. Регистратор проверяет документы (User Task)
3. **XOR**: документы полные? нет → отказ; да → дальше
4. **AND**: параллельно — проверка в базе данных (Service) и приём пошлины (Manual)
5. После — экзамен (один Task пока)
6. **XOR**: сдал? нет → ⏱ ждём 7 дней → петля; да → дальше
7. Финал — ВУ выдано

**Используй:** Start/End, Task (User/Service/Manual), Sequence Flow, XOR, AND, Timer Event.

<!--
Teacher Notes:
- Не давай готовое решение сразу. Пусть мучаются 10-15 минут.
- Ходи по аудитории, смотри что рисуют. Не исправляй сразу — отмечай, что разберёшь у доски.
- Типичные ошибки:
  1. Забывают AND-join
  2. Используют Task вместо Timer для ожидания
  3. Рисуют отказ как обычный End, хотя параллельные ветки ещё не закрыты — повод вспомнить про Terminate
- ~15 мин
-->

---

# Разбор: образец решения

```
○ Заявитель пришёл
        ↓
👤 Проверить документы
        ↓
   ◇× «Полные?»
   нет/      \да
   ◉ отказ   ↓
            ◇+ split
           /        \
       ⚙ Проверка  ✋ Принять пошлину
           \        /
            ◇+ join
              ↓
        ▢ Сдать экзамен
              ↓
        ◇× «Сдал?»
        нет/    \да
       ○⏱ 7д    ↓
         ↓     ◉ ВУ выдано
       (петля назад на экзамен)
```

> Это твой первый осмысленный BPMN. Без шуток — реальные процессы рисуют так же.

<!--
Teacher Notes:
- Разбери у доски КАЖДЫЙ элемент. Спроси у студентов, почему именно такой выбран.
- Особое внимание: AND-join (часто забывают), Timer (часто заменяют Task'ом)
- ~10 мин
-->

---

# Что дальше — BPMN-B

**В этой части мы научились:**
- Скелету: Start/End/Task/Sequence Flow
- Типам задач: User/Service/Manual/Send-Receive
- Шлюзам: XOR/AND/OR/Event-based
- Событиям: Timer/Message/Boundary Error/Signal/Terminate

**Во второй части:**
- Pool и Lanes — кто за что отвечает
- Message Flow — общение с внешними системами и организациями
- Sub-process — как прятать сложность
- Data Object, Data Store — артефакты процесса
- Loop, Group, Annotation — финальные штрихи

> Та же диаграмма выдачи ВУ продолжит расти во второй части.

<!--
Teacher Notes:
- Подведи итог. Подчеркни: одна и та же диаграмма прошла через 4 этапа и стала живой.
- Скажи: "Во второй части мы посмотрим на эту же диаграмму глазами организации — кто, кому, через какие документы"
- ~2 мин
-->
